from typing import List

from watchmen_model.admin import Pipeline, Topic


def ask_topic_monitor_rule_pipelines(topics: List[Topic]) -> List[Pipeline]:
	# TODO define all topic monitor rule pipelines
	return []
