import styled from 'styled-components';

export const IndicatorFilterContainer = styled.div.attrs({'data-widget': 'indicator-filter-edit'})`
	display        : flex;
	position       : relative;
	grid-column    : 1 / span 3;
	flex-grow      : 1;
	flex-direction : column;
`;
