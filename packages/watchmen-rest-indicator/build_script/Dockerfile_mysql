FROM python:3.9.6-slim-buster

WORKDIR /app
COPY . .
WORKDIR /app/packages/watchmen-rest-indicator
RUN pip install poetry && poetry config virtualenvs.create false && poetry lock && poetry install --no-dev -vv -E mysql -E trino

EXPOSE 8009
CMD ["uvicorn","watchmen_rest_indicator.main:app","--host", "0.0.0.0", "--port", "8009"]
