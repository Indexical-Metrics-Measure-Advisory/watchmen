[tool.poetry]
name = "watchmen-storage-rds-async"
version = "18.0.0"
description = ""
authors = ["botlikes <75356972+botlikes456@users.noreply.github.com>"]
license = "MIT"
packages = [
    { include = "watchmen_storage_rds_async", from = "src" }
]

[tool.poetry.dependencies]
python = "~3.12"
SQLAlchemy = { version = "~2.0.35", extras = ["asyncio"] }
watchmen-storage = { path = "../watchmen-storage", develop = true }
watchmen-storage-rds = { path = "../watchmen-storage-rds", develop = true }

[tool.poetry.group.dev.dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
