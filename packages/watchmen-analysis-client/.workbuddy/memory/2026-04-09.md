# 2026-04-09 Daily Log

## BI Analysis Page Performance Optimization
- Performed deep code analysis of the entire BI module (8 components, ~3000+ lines)
- Identified 10 major performance issues and applied optimizations:
  1. AlertCard: constants outside component + React.memo
  2. DataTable: paginated rendering (50 rows → load more) instead of full 500-row render
  3. ChartViews: shared `useChartKeys` hook replacing 3x duplicated logic
  4. RechartsContext: shared recharts loading via React Context (was per-instance)
  5. Extracted shared biAnalysisUtils.ts (transformMetricFlowToChartData, timeRangeToBounds)
  6. Added useCallback to 7 previously un-memoized functions in BIAnalysisPage
  7. Fixed stale closures using cardsRef.current in filter/time handlers
  8. Implemented previewCache LRU eviction
  9. Added useMemo for hasGlobalFilters in AnalysisBoard
  10. Lazy-loaded SubscriptionModal via React.lazy
- TypeScript compilation passes cleanly

## BIAnalysisPage Architecture Refactoring
- Extracted 3 custom hooks from BIAnalysisPage (was ~1700 lines → 969 lines page + 886 lines hooks):
  1. `useCardDataLoader` (274 lines) — cardDataMap, alertStatusMap, fetchCardData, loadCardDataFor, refreshCardsWithContext, refreshData, clearCardDataMap, caches
  2. `useGlobalFilters` (267 lines) — commonFilterDimensions, globalFilterValues, globalTimeRange, debounce filter handler, time range handlers, clearGlobalFilters
  3. `useAnalysisState` (345 lines) — cards, save/load/delete templates, alert dialog, drag-drop, reset, acknowledge, share link
- Key design: useCardDataLoader has NO dependency on global filter state — all context passed via options parameter to refreshCardsWithContext/loadCardDataFor
- useGlobalFilters uses ref-based state tracking (globalFilterValuesRef, globalTimeRangeRef, etc.) to avoid stale closures
- MetricBuilder/Preview logic remains in BIAnalysisPage (local UI state, not extracted)
- AlertConfigurationModal dedup still pending (cancelled for now)
- TypeScript compilation passes cleanly
